# Optional: Google Drive service account JSON (base64-encoded)
GOOGLE_DRIVE_SERVICE_ACCOUNT_JSON_BASE64=
# Optional: Restrict Drive search to a specific folder
GOOGLE_DRIVE_FOLDER_ID=

# Optional: Google Maps Static Street View API key (for image fallback)
GOOGLE_MAPS_API_KEY=

# Optional: People Data Labs or other skip tracing provider key
PDL_API_KEY=
